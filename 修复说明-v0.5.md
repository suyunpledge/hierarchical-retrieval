# v0.5 优化说明

> 2026-09-02。依据《本地检索系统 v0.4 二轮审查结论与修改方案》（2026-09-01，Claude 审查 → 千问整理为 docx）实施两个严重性能问题的修复，版本号 0.4.0 → 0.5.0。

## 一、改版范围总览

| 类别 | 项目 |
|------|------|
| 性能修复 | P1（search_in_entry 正排索引，替代全库暴力扫描）、P2（VectorStore 增量落盘 + 脏标记 + flush） |
| 配套 | api_server /ingest 端点加 flush 兜底；版本号三处同步 0.5.0 |
| 文档 | 修复说明-v0.5.md 新增 |

## 二、修复清单

| ID | 级别 | 问题 | 修复 | 位置 |
|----|------|------|------|------|
| P1 | 严重 | v0.4 N5 为保证召回把 search_in_entry 的检索窗口放大到全库（top_k = max(count, top_k)），每次条目内检索都是 O(全库向量数) 的暴力扫描，库增长后成为主检索路径瓶颈 | 维护正排索引 entry_id → [faiss_id...]（随 metadata 持久化，旧库缺失时从 metadata 惰性重建），新增 search_by_entry()：取该条目全部向量做 numpy 内积排序取 top-k，复杂度降到 O(该条目向量数)；cloud_cache.search_in_entry 改用之，回退逻辑保留 | storage/vector_store.py, core/cloud_cache.py |
| P2 | 严重 | v0.4 N3 把落盘放进锁内（原子写 + RLock），add()/update_metadata()/delete() 每次操作都在锁内全量重写 FAISS 索引与 metadata.json，I/O 放大且锁竞争加剧 | 写操作改为只改内存 + 置脏标记 self._dirty，不再直接落盘；新增 flush()（锁内原子刷盘）；add_many() 保留批量后落盘一次（ingest 批次边界）；/ingest 端点处理完统一 flush 兜底；语义为"最终一致"，崩溃最多丢失最近一批未刷新的更新 | storage/vector_store.py, api_server.py |

## 三、验证记录（2026-09-02，本机实测）

- 全部 .py 通过 py_compile 编译；
- 正排索引版检索：query=「抱抱 泡泡」→ 关键句 5 条（最高 0.789）+ 全文段 4 段（最高 0.889），与 v0.4 全库扫描版结果一致（正确性不回归）；
- 话题域检测正常（td_af834aa7，相似度 0.82）；
- 服务重启后 FAISS/metadata 从磁盘正常恢复（含 entry_to_ids 字段）；
- 旧库兼容：无 entry_to_ids 字段的历史 metadata 加载时自动从 metadata 惰性重建正排索引。

## 四、遗留（继承 v0.4 路线图，本轮未动）

- M4（ingest 中途失败重试建关键句）、M5.2/M5.3/M8（IndexIDMap2 物理删除）/M9/M10/M11：按路线图择期；
- 中等问题（LocalStorage 并发语义声明、assign_domain 锁内线性扫描）：当前规模无感知，记录待后续优化；
- 已知限制：VectorStore 标记删除（向量不物理删除），P2 待 IndexIDMap2 解决。

---

## 五、补充修复（2026-09-02 二轮审查后）

依据《本地检索系统 v0.5 审查补充意见》（Claude App 审查，未走 API），确认 v0.5 两项优化均已采纳且实现方向正确；并修复审查发现的两个问题：

| ID | 级别 | 问题 | 修复 |
|----|------|------|------|
| R1 | 严重 | _load_index 惰性重建 _entry_to_ids 时，若 _metadata 有记录但 _id_map 缺失（历史脏数据），会塞入 -1，之后 search_by_entry 调 reconstruct(-1) 直接崩溃 | 重建时过滤：fid = self._id_map.get(ext, -1); if fid >= 0: ...append(fid) |
| R2 | 中等 | flush() 只挂在 /ingest 端点，update_metadata()/delete() 的脏标记可能永远不落盘（直接作为 Python 包调用时） | 两个低频写操作在锁外追加 flush() 兜底落盘（频率低，全量重写代价可接受） |

验证：两个文件 py_compile 通过；服务重启后检索正常（正排索引正确性不回归）。
