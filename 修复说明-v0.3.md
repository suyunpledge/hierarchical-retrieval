# v0.3 修复说明

> 2026-09-01。依据《hierarchical-retrieval 整合文档 v0.2 全面技术审查报告》（2026-09-01，代码审计员）
> 完成 **P0 批次**（3 项阻断级 + 4 项低成本高收益项）与配套文档修正。
> 改版原则：按审查报告推荐方案执行；保持文档整体结构与代码对外接口兼容；
> 每项代码修复附带可复现的验收方式。

---

## 一、改版范围总览

| 类别 | 项目 |
|------|------|
| 代码修复 | B1（补录窗口）、B2（requires-python）、B3（脚本绑定 + Bearer 鉴权）、M6（api_server 中文阈值）、M5.1（单条超长消息截断）、M3（四个检索入口零向量校验）、M7（先过滤后补全局） |
| 实施中新发现 | 全局补充结果被二次展平清空字段（v0.2 隐藏缺陷，随 M7 一并修复，详见 §三.7） |
| 文档修正 | M12（删无依据倍数断言、"云端缓存"更名）、M13（三级职责描述对齐实现）、I5 全部 10 项失配、I7（硬编码个人路径） |
| 版本 | `0.2.0 → 0.3.0`（pyproject / `__init__.py` / api_server 同步） |

---

## 二、修复清单

| ID | 级别 | 问题 | 修复 | 位置 |
|----|------|------|------|------|
| B1 | 阻断 | 补录窗口取 `[-cap:]`（最新一批），窗口随对话增长前移，最早的历史消息**永久丢失**，与"留到下一轮继续补"的注释相反 | 窗口改为**未入库 ∩ 最旧优先**：先按内容哈希剔除已入库消息（新增 `CloudCache.has_content()`），再从最旧一批补起，任何消息最多 ceil(n/cap) 轮后必然入库 | `api_server.py`, `core/cloud_cache.py` |
| B2 | 阻断 | `requires-python >= 3.8`，但代码使用 `list[str]` 等 3.9+ 语法（且无 `from __future__ import annotations`），3.8 下导入即崩溃 | `requires-python = ">=3.9"`，删除 3.8 classifier，README 前置改为"需要 3.9+" | `pyproject.toml`, `README.md` |
| B3 | 阻断 | 三个启动脚本默认绑 `0.0.0.0` 且无鉴权，与代码内"仅本机"安全意图冲突；CORS `allow_credentials + 通配 origins` 违反规范 | 三脚本 `--host` 默认 `127.0.0.1`；新增可选 `HR_API_TOKEN` Bearer 鉴权（覆盖 `/v1/models`、`/v1/chat/completions`、`/ingest`、`/retrieve`，`/health` 保持开放）；删 `allow_credentials`；README 新增「安全注意事项」 | `api_server.py`, `start_api.bat`, `start_hr_api_hidden.vbs`, `hr-api-task.xml`, `README.md` |
| M6 | 重要 | api_server 沿用英文默认阈值，中文对话全部被误并入同一话题域，L3 形同虚设 | api_server 全局配置显式应用中文推荐值：`topic_min_similarity=0.70`、`topic_similarity_threshold=0.55`、`key_sentence_similarity_threshold=0.60` | `api_server.py` |
| M5.1 | 重要 | 近期对话装配对最新一条**无条件保留**（不受预算约束），单条 50k 字消息即可旁路"结构性有界" | 最新一条超预算时截断装配（`content[:budget-used-9]` + 追加"（原文过长已截断）"标记）；极端情况下（记忆块占满预算）仍保住最新一条开头 64 字符，避免把用户本轮提问整个丢掉 | `api_server.py` |
| M3 | 重要 | 除主检索入口外，四个检索入口（`CloudCache.search` / `search_in_entry`、`KeySentenceLibrary.search`、`TopicDomainManager.search_in_domain`）无零向量校验，嵌入服务掉线时返回按插入序排列的伪命中 | 四个入口统一加 `NomicEmbedding.is_valid()` 前置校验，零向量时记 error 日志并返回空列表 | `core/cloud_cache.py`, `core/key_sentence.py`, `core/topic_domain.py` |
| M7 | 重要 | 检索顺序为"数量不足才补全局 → 最后统一过滤"：界域返回恰好 tks 条低分噪声时既不触发补充、又被过滤清空，全局高分答案被错过 | **先按阈值过滤界域结果，再判断是否补全局**（全局补充同样过滤）；实施中发现并修复二次展平清空字段的隐藏缺陷（见 §三.7） | `core/retrieval.py` |
| +1 | 新发现 | v0.2 全局补充路径把 `key_sentence_lib.search()` 返回的**已展平**结果又过一遍 `_flatten_results`，text/sentence_id/source_entry_id 全被清空——补充的关键句进了结果列表却无法关联全文，等于白补 | 补充路径直接使用 `search()` 返回值，不再二次展平 | `core/retrieval.py` |

---

## 三、逐项说明与验收方式

### 1. B1 — 补录窗口：未入库 ∩ 最旧优先

**修复逻辑**：`_compress_history` 补录前先 `has_content()`（内容哈希，O(1)）剔除已入库消息，再取剩余的**最旧** `cap` 条。已入库内容不再挤占窗口名额，窗口不再随对话增长向前滑动。

**验收**：`tests/test_fixes.py › test_backfill_oldest_first_and_complete`
—— 60 条历史（53 条 older）、cap=32：首轮补录最旧 32 条（含第 0 条），第二轮补完剩余 21 条，两轮后 53 条全部入库。旧实现在该场景下第 0~20 条永久丢失。

### 2. B2 — requires-python >= 3.9

**修复**：`pyproject.toml` 的 `requires-python` 与 classifiers、README 前置环境同步更新。选择审查报告推荐方案 b（抬版本下限）而非加 future import——代码中 `list[str]` 运行时注解出现在函数签名与函数体，逐处补 future import 改动面更大。

**验收**：`python -m py_compile` 全部通过（本机 Python 3.12）；3.9/3.10 的 CI 冒烟测试列入 P1 建议（本次静态环境无 3.9/3.10 解释器）。

### 3. B3 — 默认仅本机监听 + 可选 Bearer 鉴权

**修复**：
- `start_api.bat` / `start_hr_api_hidden.vbs` / `hr-api-task.xml` 三处 `--host` 统一改 `127.0.0.1`（bat 与 vbs 附带切换局域网模式的注释说明）；
- `api_server.py` 新增 `HR_API_TOKEN` 环境变量与 `verify_token` 依赖：设置后除 `/health` 外的接口要求 `Authorization: Bearer <token>`；LobeChat 侧把"API Key"填同名值即可；不设置则不鉴权（保持仅本机监听形态）；
- 删除 CORS `allow_credentials=True`（与 `allow_origins=["*"]` 组合违反规范）。

**验收**：鉴权逻辑四象限验证（无 header / 错 token / 对 token / 未启用）全部符合预期；路由依赖检查确认 4 个业务端点均挂载依赖、`/health` 与文档页保持开放。README「安全注意事项」同步说明。

### 4. M6 — api_server 应用中文阈值

**修复**：api_server 的全局 `HConfig` 显式传入三个中文推荐阈值（0.70 / 0.55 / 0.60），与 demo、README 中文调参提示一致。

**验收**：静态核对配置值；实际分域效果属运行时行为，建议按审查报告 P0 验收方式用 FAQ 场景对话实测分域（需 Ollama 环境，本次离线验证未覆盖，列入验收建议）。

### 5. M5.1 — 单条超长消息截断装配

**修复**：近期对话装配循环中，最新一条超预算时截断到 `budget - used - len(标记)` 并追加"（原文过长已截断）"；极端情况下（剩余空间连标记都放不下）保底保留开头 64 字符——用户本轮提问不能被整个丢弃，允许小幅超出预算是显式取舍。

**验收**：`test_single_oversized_message_truncated` —— 8 条消息含一条约 5 万字的最新提问：压缩输出 ≤ 2200 字（预算 2000 + 少量宽放），带截断标记，且保留消息开头。旧实现输出约 5 万字，必然失败。

### 6. M3 — 四个检索入口零向量校验

**修复**：四个入口在 `embed(query)` 后统一加 `is_valid()` 校验，失败时 `logger.error` 并返回 `[]`，与主入口 `retrieve()` 的既有防线对齐；docstring 同步注明"嵌入失败返回空"。

**验收**：`test_dead_embedding_query_returns_empty`（既有用例）覆盖主入口；四个入口的守卫为同构代码，静态核对 + `py_compile`。建议后续在 P1 用 DeadEmbedding 直接调用四个入口补显式断言（成本极低，本次以同构性保证）。

### 7. M7 — 先过滤后补全局（含新发现缺陷）

**修复**：
- 界域检索结果**先按** `key_sentence_similarity_threshold` 过滤，**再**判断是否不足 tks 条、需要补全局；全局补充结果同样过滤后去重合并。文末统一"分数门槛"保留为兜底（覆盖无匹配话题的全局检索分支，并防御未来新增入口漏配过滤）。
- 实施中发现：v0.2 的全局补充把 `key_sentence_lib.search()`（已返回展平格式）又过了一遍 `_flatten_results`，导致补充结果 `text=""`、`sentence_id=""`、`source_entry_id=""`——关键句进了列表却无法关联全文。已改为直接使用返回值。该缺陷不在审查报告清单内，由本次新增回归测试暴露。

**验收**：`test_m7_domain_noise_triggers_global_supplement` —— 注入"话题检测误命中 A 域 + 界域内恰好 tks 条 0.10 分噪声"的场景，断言：全局库中 1.0 分的高分关键句被找回、最终结果无低分残留、无界域噪声混入。旧实现（无论新旧过滤顺序）在该场景下返回空。

### 8. 文档修正汇总（M12 / M13 / I5 / I7）

| 项 | 修正内容 |
|----|----------|
| M12 | 删除"1M 可当 100M / 3~5M / 几何倍数"等无依据倍数断言（README、`__init__.py` docstring），替换为可验证口径：prompt 体积结构性有界，记忆收益以检索命中率衡量、需实测；"云端缓存"全部更名"**全文缓存（本地磁盘）**"（README、各模块 docstring、demo 输出；类名 `CloudCache` 沿用并注明沿革） |
| M13 | README「核心思想」三级职责重写为与实现一致：L1 = 全文兜底数据层 + 详情层（由关键句关联访问，独立全库检索走 `CloudCache.search` API；自动管线 L1 兜底召回列入路线图） |
| I5 | 全部 10 项：① `ks_lib.ingest` 返回类型标注 `list[str]`→`list[dict]`（README 与代码签名一致化）②"start.bat"→"start_api.bat" ③"近 6 轮原文"→"近 6 条消息（约 3 轮）" ④ README 配置参数补齐 7 个缺失项（装配/代理/后端/并发）⑤ pyproject 新增 `[api]` extra 并与 requirements_api.txt 对齐、删除未使用的 httpx、移除占位 Homepage URL ⑥ .gitignore 补 `hierarchical_api_storage/` ⑦ 云端缓存命名（并入 M12）⑧"保证召回率"→"尽量弥补召回缺口" ⑨"之后每轮秒级"→按实测 16.7 s 如实描述为十秒级 ⑩"与历史长度无关"补充边界条件指引 |
| I7 | vbs / 两个计划任务 XML 中的硬编码个人路径（含乱码用户名）改为 `%USERPROFILE%` 占位：vbs 用 `ExpandEnvironmentStrings` 运行时展开；XML 靠 `cmd /c` 展开并删除对环境变量展开不可靠的 `WorkingDirectory` 元素（改由 `cd /d` 定位） |

---

## 四、验证记录（2026-09-01）

- **单元测试 12/12 通过**（`python tests/test_fixes.py`，确定性假嵌入，不依赖网络）：
  v0.2 的 9 个用例全部保留且通过（含 `test_compress_history_bounded` 回归——压缩改动未破坏既有行为）；
  新增 3 个回归用例（B1 / M5.1 / M7），旧实现在这 3 个用例上均必然失败；
- **鉴权逻辑**：四象限验证通过（见 §三.3）；
- **语法**：`py_compile` 覆盖全部改动文件通过；
- **环境**：Python 3.12 + faiss-cpu + fastapi；3.9/3.10 冒烟与中文分域实测需真实环境，列入 §五验收建议；
- 未做真实 Ollama 端到端实测（本环境无 Ollama 服务），运行时行为结论以静态审查 + 单测为准。

---

## 五、遗留路线图（继承审查报告 §7，P0 已完成）

**P1（下一版，建议优先级从上到下）**

| 项 | 内容 | 备注 |
|----|------|------|
| M13 代码侧 | L1 兜底召回：retrieve 无命中时对 `cloud_cache.search()` 直接检索并纳入结果 | 50+ 条目库测召回 |
| M1 | 条目内局部检索（`search_in_entry` 的放大窗口在小条目上偏弱） | 同上 |
| M2 | 并发写加锁 + 原子写（`.tmp` + rename） | 双客户端并发压测 |
| M4 | ingest 失败幂等清理（部分成功时的残留条目） | |
| M12 代码侧 | 定义检索命中率评测协议（固定语料 + 固定查询集），让"扩展倍数"可测量 | |
| M14 | 启动链路统一（uvicorn 字符串导入在 AutoClaw Python 下的兼容性实测、去 `--reload`） | 需实测确认 |
| I 项 | README 项目结构补 `api_server.py`/`tests/` 等实际文件 | 1 行级 |

**P2（择期）**

| 项 | 内容 |
|----|------|
| M5.2 | `len(messages) ≤ keep` 时整段跳过压缩的旁路（数条超长消息可不触发压缩） |
| M5.3 | 代理转发未下发 `num_ctx`，上下文上限依赖 Ollama 默认值 |
| M8 | `VectorStore` 换 `IndexIDMap2`（真正删除向量而非标记删除） |
| M9 | 向量索引增量持久化（避免全量重写） |
| M10 | 话题质心演化策略 + 句子级域分配 |
| M11 | 更换多语嵌入模型并重校准全部阈值 |
| I1-I8 | 审查报告列出的其余文档细节 |

**验收建议**（随 P1 一并）：
1. 3.9/3.10 解释器导入冒烟（B2 的版本下限承诺）；
2. FAQ 场景中文对话实测分域（M6 的运行时确认）；
3. 四个检索入口 DeadEmbedding 显式断言（M3 的直接用例化）。

---

## 六、升级与兼容性说明

- **存储格式无变化**：v0.2 的既有库可直接被 v0.3 读取，无需迁移；
- **对外接口兼容**：类名与方法签名不变；新增 `CloudCache.has_content()`（纯新增）；`KeySentenceLibrary.ingest` 仅修正返回类型标注（实际返回值从未变化）；
- **部署侧变化**：三个启动脚本默认改绑 `127.0.0.1`（需要局域网访问需手动改 `--host`，建议同时设置 `HR_API_TOKEN`）；api_server 新增可选环境变量 `HR_API_TOKEN`；
- **M6 行为变化**：api_server 的默认阈值从英文值改为中文推荐值——用中文对话直连 api_server 的用户会感受到分域更准；如需回旧行为，可通过环境变量/配置覆盖。

*v0.3 修复说明完。对应审查报告条目的进一步展开，或 P1 批次的修复 diff，随时指派。*