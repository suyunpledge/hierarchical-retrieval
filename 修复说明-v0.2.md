# v0.2 修复与新增说明

> 2026-08-29。对应《代码审查报告.md》中 #1–#6 号问题的修复，以及一个新增的核心能力：
> **OpenAI 兼容对话代理**——它回答了"为什么接到 LobeChat 里对上下文没有任何扩展"。

---

## 一、为什么之前"接了个寂寞"

LobeChat 这类客户端是**全量重放**模式：每轮请求都把完整历史塞进 prompt 发给模型。
把分级检索挂在旁边（ingest 钩子、手动 /retrieve）**不会改写发出去的 payload**，
历史该多长还是多长——本地小模型照样溢出。检索系统不在对话链路上，就等于没接。

再叠加两个 bug 让手动检索也拿不到好结果：
- `source_entry_id` 收集后从未使用，"全文关联"实际是全库重新搜索，结果与命中的关键句无关；
- Ollama 掉线时嵌入静默返回零向量，索引被污染后检索返回按插入序排列的伪命中。

## 二、修复清单

| # | 问题 | 修复 | 位置 |
|---|------|------|------|
| 1 | 全文关联未实现 | `search_in_entry()`：在关键句**所属条目内**检索段落，条目无命中时回退原文；`full_contexts` 严格来自命中关键句的来源 | `core/cloud_cache.py`, `core/retrieval.py` |
| 2 | 零向量静默污染 | `embed(strict=True)` 写入路径失败即抛错（先全部嵌入成功再落盘）；查询路径零向量时返回空结果并明确提示 | `core/embedding.py`, 各写入点 |
| 3 | 分数门槛未生效 | 低于 `key_sentence_similarity_threshold` 的关键句一律丢弃 | `core/retrieval.py` |
| 4 | 话题域无上限 | 达到 `topic_max_domains` 后强制归入最相似域 | `core/topic_domain.py` |
| 5 | 写入 O(n²) IO | `add_many()` 批量入索引；`embed_batch()` 并发嵌入；关键句向量复用（原来每句嵌两遍） | `storage/vector_store.py`, `core/embedding.py` |
| 6 | 长对话只留 3 句 | 按 chunk 提取关键句，每块 N 句 | `core/key_sentence.py` |
| + | 重复入库膨胀 | 内容 sha256 去重 + `ingest_result` 落档，ingest 幂等 | `core/cloud_cache.py` |

## 三、新增：OpenAI 兼容代理（`/v1/chat/completions`）

LobeChat ──完整历史──▶ hr 代理 ──压缩──▶ 记忆块(≤3000字) + 近 6 条消息(约 3 轮) ──▶ Ollama 对话模型
│
└─ 旧消息自动入库（分级检索），单次最多补录 32 条

- 发出去的 prompt **从结构上就有界**（默认 ≤ 约 6000 字），与历史长度无关
  （v0.3 起单条超长消息也会被截断装配；len≤keep 不压缩的旁路等边界情况
  见修复说明-v0.3）；
- 支持 `/v1/models` 探测、非流式与 SSE 流式；
- 环境变量：`HR_CHAT_MODEL`（回退模型）、`HR_CHAT_BACKEND`（后端地址）、
  `HR_STORAGE_ROOT`、`HR_OLLAMA_URL`。

### LobeChat 接入步骤

1. 启动本服务：`start_api.bat`（或 `python api_server.py`，监听 127.0.0.1:8000）；
2. LobeChat → 设置 → 语言模型 → 添加 **OpenAI** 兼容服务商：
   - API 地址：`http://localhost:8000/v1`
   - API Key：任意非空字符串
   - 模型：填本地 Ollama 的对话模型名（如 `glm4:9b`、`qwen3:8b`）
3. 新会话选该服务商对应模型即可。旧的超长对话首轮会较慢（补录入库，
   实测 30 条约 135 s）；之后每轮只有增量（实测同请求复跑 16.7 s，
   属十秒级而非秒级，随库规模与消息长度变化）。

## 四、验证记录（2026-08-29，本机实测）

- 单元测试 9/9 通过（`tests/test_fixes.py`，确定性假嵌入，不依赖网络）；
- 端到端（真实 nomic-embed-text + glm4:9b）：
  - 关联正确性：`full_contexts` 的 entry_id ⊆ 命中关键句的来源集合 ✓
  - **压缩回路**：34 条 / 3344 字历史 → 8 条 / 1042 字（记忆块 469 字）→
    模型正确答出 28 轮之前的事实（"您的猫叫雪球……冻干鸡肉"）✓
  - 冷启动 30 条旧消息补录约 135 s（此后去重、增量秒级）；同请求复跑 16.7 s ✓
  - SSE 流式含 `[DONE]` ✓

## 五、遗留与调优建议

1. **中文阈值**：nomic-embed-text 对中文余弦相似度普遍偏高（闲聊与正题相似度可到 0.9），
   建议 `topic_min_similarity=0.70`、`topic_similarity_threshold=0.55`、
   `key_sentence_similarity_threshold=0.60` 起步再按数据调；
2. 关键句提取仍是词频启发式（构思原文是"AI 提炼"）——可接本地小模型做语义提取，
   这同时是"算力端侧下放"的示范场景；
3. `VectorStore.delete` 仍是标记删除（向量留存索引中）；并发写仍未加锁——
   单用户场景够用，多客户端同时写入前需要补锁；
4. 冷启动大批量补录可进一步异步化（后台线程写入，首答不等待）。