# v0.4 优化说明

> 2026-09-01。在 v0.3 之上的一轮全面代码审查与修复。本轮重点：
> **并发安全、检索召回、目录结构对齐打包配置、代理参数透传**。
> 全部 12 个既有回归测试通过（Python 3.13 + faiss-cpu 1.15 + 真实 Ollama 环境）。

## 一、修复清单

| ID | 级别 | 问题 | 修复 | 位置 |
|----|------|------|------|------|
| N1 | 阻断 | 目录结构与 `pyproject.toml` 不符：源码平铺在仓库根目录，而打包配置期望 `hierarchical_retrieval/` 包目录。`pip install .` 打出空包，`tests/test_fixes.py` 因找不到包根直接抛 RuntimeError | 源码整体迁入 `hierarchical_retrieval/`（`__init__.py`、`config.py`、`py.typed`、`core/`、`storage/`、`utils/`、`examples/`），与 `[tool.setuptools.packages.find]` 的 `include = ["hierarchical_retrieval*"]` 对齐 | 仓库结构 |
| N2 | 重要 | `LocalStorage` 非原子写、无锁：进程崩溃可留半截 JSON；并发写互相覆盖 | 临时文件 + `os.replace` 原子替换；实例级 `RLock` 串行化写/删 | `storage/local_storage.py` |
| N3 | 重要 | `VectorStore` 非原子写、无锁：`add/add_many` 与 `search` 并发时 FAISS 索引与 metadata 可能不一致；崩溃可损坏 metadata.json | `_save_index` 改临时文件 + `os.replace`；`add/add_many/search/update_metadata/delete` 全部纳入 `RLock` 临界区 | `storage/vector_store.py` |
| N4 | 重要 | `CloudCache` 内容去重索引并发读写无保护，且非原子写（该索引一丢，去重与 B1 补录判定全部失效） | 索引读写加 `_content_lock`；落盘改临时文件 + `replace`；`has_content()` 同锁 | `core/cloud_cache.py` |
| N5 | 重要 | `search_in_entry()` 固定取全库 Top `max(top_k*4, 16)` 再按 entry_id 过滤：库变大后指定条目的真实段落挤不进全库前 16 名，被误判"无命中"而回退原文开头，M1 缺陷在更大库规模下复发 | 检索窗口改为覆盖全部活跃向量（`top_k=max(index_count, top_k)`），保证条目内结果不被全库排名挤掉 | `core/cloud_cache.py` |
| N6 | 重要 | `TopicDomainManager.assign_domain` 匹配→质心更新→新建三步无锁：FastAPI 线程池并发 ingest 时可能创建重复话题、丢失 entry_count/质心更新 | 嵌入完成后进入 `RLock` 临界区，注册表变更整体串行化（拆出 `_assign_domain_locked`） | `core/topic_domain.py` |
| N7 | 重要 | `import api_server` 即构建真实引擎：每次跑测试都会触发 Ollama 健康检查并在 cwd 创建 `hierarchical_api_storage/`（测试已 chdir 到临时目录才没污染真实库，属侥幸） | 引擎改惰性单例 `get_hr()`；测试仍可对 `api_server.hr` 注入替身。副作用：测试不再要求先 chdir | `api_server.py` |
| N8 | 一般 | `/v1/chat/completions` 把 `temperature` 放在 Ollama 请求顶层（Ollama 只认 `options.temperature`，参数被静默丢弃）；`max_tokens` 完全没有透传 | `temperature → options.temperature`，`max_tokens → options.num_predict` | `api_server.py` |
| N9 | 一般 | `start_api.bat` 仍带 `--reload`：常驻服务开热重载会双进程、重复初始化引擎、放大并发写风险 | 移除 `--reload` | `start_api.bat` |
| N10 | 一般 | `hr-api-task.xml` 实际仍绑 `0.0.0.0` 且硬编码 `C:\Users\<用户名>`（v0.3 文档宣称已修复，但该文件漏改）；`ollama-task.xml` 同样硬编码路径；两文件为 UTF-16 声明 + UTF-8 BOM 的错编码组合 | API 任务改绑 `127.0.0.1`；两个 XML 的路径全部改 `%USERPROFILE%`（`cmd /c` 展开）；统一重写为真正的 UTF-16-LE+BOM；移除失效的 `WorkingDirectory`（改 `cd /d`）；`[xml]` 解析验证通过 | `hr-api-task.xml`, `ollama-task.xml` |
| N11 | 一般 | `start_hr_api_hidden.vbs` 的 `hrDir` 指向 `%USERPROFILE%\Desktop\hierarchical_retrieval`，与实际目录 `Desktop\本地工作流\hierarchical_retrieval` 不符（v0.3 改占位符时写错了目标） | 修正为实际路径；文件统一为 UTF-16-LE（WSF/VBS 规范编码） | `start_hr_api_hidden.vbs` |
| N12 | 文档 | 版本号 | `pyproject.toml` / `__init__.py` / api_server 同步 `0.3.0 → 0.4.0` | 三处 |

## 二、明确不改的（审查结论记录）

- `Search` 路径与 M3 零向量防线：已有且测试覆盖，不动。
- `VectorStore` 标记删除（不物理删向量）：P2/M8 已规划 `IndexIDMap2`，本轮不抢做。
- FAISS 持久化仍是全量重写：P2/M9 范畴，当前库规模（百级条目）无感知。
- `embed_batch` 的 `ThreadPoolExecutor` 每 ingest 新建：成本可忽略，不为改而改。

## 三、验证记录（2026-09-01，本机实测）

- `python tests/test_fixes.py`：**12/12 通过**（真实 Ollama 在线，压缩用例走真实嵌入链路）；
- 全部 `.py` 通过 `py_compile` 与 AST 解析；
- `LocalStorage` 原子写 + 中文内容 + 无 `.tmp` 残留：通过；
- `VectorStore` 批量写入、检索排序、跨实例重载 count：通过；
- 两个计划任务 XML：UTF-16 解析 + 结构校验通过，确认无 `0.0.0.0`、无硬编码 `C:\Users\`；
- 端到端冒烟（假嵌入双话题写入→检索→status）：通过。

## 四、遗留（继承 v0.3 路线图，本轮未动）

- M4：ingest 中途失败（L3 成功、L2 失败）会留下"有全文有关键句但无落档结果"的条目，重试会重复建关键句 —— 下一批处理；
- M5.2 / M5.3 / M8 / M9 / M10 / M11：按 v0.3 路线图择期。

*v0.4 优化说明完。*
